{
  "name": "@syncpty/web-client",
  "version": "0.1.0",
  "private": true,
  "description": "Web client for SyncPTY",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@syncpty/transport": "workspace:*",
    "@syncpty/protocol": "workspace:*",
    "xterm": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
