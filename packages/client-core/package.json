{
  "name": "@syncpty/client-core",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime-agnostic client session runtime for SyncPTY, shared between CLI and browser clients.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@syncpty/protocol": "workspace:*",
    "@syncpty/transport": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
